{
  "info": {
    "name": "CommitRecap Backend API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000"
    },
    {
      "key": "username",
      "value": "octocat"
    },
    {
      "key": "perPage",
      "value": "100"
    },
    {
      "key": "page",
      "value": "1"
    }
  ],
  "item": [
    {
      "name": "Issue Count (Author, Date Range)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/issues?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "issues"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "PR Count (Author, Date Range)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/prs?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "prs"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "PR Count Reviewed by User (Date Range)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/prs-reviewed?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "prs-reviewed"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "Merged PR Count (Date Range)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/prs-merged?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "prs-merged"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "Commit Count (Author, Date Range)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/commit-count?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "commit-count"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "Commit Count Monthly (2025)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/commit-count-monthly-2025?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "commit-count-monthly-2025"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "Repo Count (Public)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/repo-count?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "repo-count"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "User Summary",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/user-summary?username={{username}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "user-summary"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            }
          ]
        }
      }
    },
    {
      "name": "Rate Limit",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/rate-limit",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "rate-limit"
          ]
        }
      }
    },
    {
      "name": "Top Languages by Repo Stars",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/top-languages-by-stars?username={{username}}&per_page={{perPage}}&page={{page}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "top-languages-by-stars"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            },
            {
              "key": "per_page",
              "value": "{{perPage}}"
            },
            {
              "key": "page",
              "value": "{{page}}"
            }
          ]
        }
      }
    },
    {
      "name": "Fetch Most Used Languages",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/github/search/languages?username={{username}}&per_page={{perPage}}&page={{page}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "github",
            "search",
            "languages"
          ],
          "query": [
            {
              "key": "username",
              "value": "{{username}}"
            },
            {
              "key": "per_page",
              "value": "{{perPage}}"
            },
            {
              "key": "page",
              "value": "{{page}}"
            }
          ]
        }
      }
    }
  ]
}
